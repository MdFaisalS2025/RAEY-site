import type { NextConfig } from "next";

// Baseline security headers for a public site with no CSP-breaking needs
// (no third-party embeds beyond the fonts/OG images Next already serves
// from its own origin). frame-ancestors 'none' + X-Frame-Options both set
// since older browsers only honor the header, not the CSP directive.
const securityHeaders = [
  { key: "X-Content-Type-Options", value: "nosniff" },
  { key: "X-Frame-Options", value: "DENY" },
  { key: "Referrer-Policy", value: "strict-origin-when-cross-origin" },
  { key: "Permissions-Policy", value: "camera=(), microphone=(), geolocation=()" },
  {
    key: "Strict-Transport-Security",
    value: "max-age=63072000; includeSubDomains; preload",
  },
];

const nextConfig: NextConfig = {
  async headers() {
    return [
      {
        source: "/:path*",
        headers: securityHeaders,
      },
    ];
  },
};

export default nextConfig;
